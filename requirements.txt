# Python dependencies for the backend.
# Authoritative source is backend/pyproject.toml (managed by uv).
# This file exists so teammates can also run: pip install -r requirements.txt
#
# Node/npm deps live in frontend/package.json.

# ── Backend runtime ─────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0
aiosqlite>=0.20.0
pydantic>=2.0
pydantic-settings>=2.0
httpx>=0.27.0
pandas>=2.2.0
numpy>=1.26.0
scikit-learn>=1.5.0
langchain>=0.2.0
langchain-openai>=0.1.0
langchain-core>=0.2.0
openai>=1.30.0
python-dotenv>=1.0.0
rich>=13.0.0

# ── Backend dev ─────────────────────────────────────────
ruff>=0.4.0
mypy>=1.10.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
